//my answer
const solution = (num1, num2) => Math.floor((num1 / num2) * 1000);

//most liked answer
function solution(num1, num2) {
  return Math.trunc((num1 / num2) * 1000);
}
//Math.trunc()는 주어진 값으 소수 부분을 제거하고 숫자의 정수 부분을 반환
